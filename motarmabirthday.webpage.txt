<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Happy Birthday, Mohtarma Ji!</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      overflow: hidden;
      text-align: center;
      background: linear-gradient(45deg, #ff69b4, #ff1493, #ff6347, #ffd700, #00bfff);
      background-size: 400% 400%;
      animation: gradientAnimation 10s ease infinite; /* Smooth gradient animation */
    }

    @keyframes gradientAnimation {
      0% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
      100% {
        background-position: 0% 50%;
      }
    }

    .container {
      position: relative;
      z-index: 1;
      margin-top: 50px;
    }

    h1 {
      color: #ffeb3b;
      font-size: 3em;
      margin-bottom: 10px;
    }

    p {
      font-size: 1.5em;
      color: #fff;
      margin-top: 20px;
    }

    /* Heart Shape Background (Big Heart) */
    .big-heart {
      position: absolute;
      top: 20%;
      left: 50%;
      width: 300px;
      height: 300px;
      background-color: rgba(255, 20, 147, 0.6);
      transform: rotate(-45deg);
      z-index: 0;
      border-radius: 50%;
      animation: pulse 2s ease-in-out infinite;
    }

    /* Pulse animation for large heart */
    @keyframes pulse {
      0% {
        transform: scale(1) rotate(-45deg);
      }
      50% {
        transform: scale(1.1) rotate(-45deg);
      }
      100% {
        transform: scale(1) rotate(-45deg);
      }
    }

    /* Small Heart Elements */
    .small-heart {
      position: absolute;
      width: 30px;
      height: 30px;
      background-color: #ff1493;
      transform: rotate(-45deg);
      animation: beat 1s infinite, floatHeart 5s ease-in-out infinite;
    }

    /* Heart beat animation */
    @keyframes beat {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.3);
      }
      100% {
        transform: scale(1);
      }
    }

    /* Floating Heart Animation */
    @keyframes floatHeart {
      0% {
        transform: translate(0, 0) rotate(-45deg);
      }
      50% {
        transform: translate(40px, -40px) rotate(-45deg);
      }
      100% {
        transform: translate(0, 0) rotate(-45deg);
      }
    }

    /* Positioning small hearts around the big heart */
    .small-heart:nth-child(1) {
      top: 5%;
      left: 25%;
    }

    .small-heart:nth-child(2) {
      top: 20%;
      left: 40%;
    }

    .small-heart:nth-child(3) {
      top: 50%;
      left: 55%;
    }

    .small-heart:nth-child(4) {
      top: 70%;
      left: 30%;
    }

    .small-heart:nth-child(5) {
      top: 85%;
      left: 60%;
    }

    /* Balloon Styles */
    .balloon {
      position: absolute;
      bottom: -100px;
      width: 50px;
      height: 70px;
      border-radius: 50%;
      background-color: #ff6347;
      animation: floatBalloon 10s linear infinite;
    }

    .balloon:nth-child(odd) {
      background-color: #ff6347; /* Red balloons */
    }

    .balloon:nth-child(even) {
      background-color: #ff1493; /* Pink balloons */
    }

    /* Balloon Animation */
    @keyframes floatBalloon {
      0% {
        transform: translateX(0) translateY(0);
      }
      100% {
        transform: translateX(200px) translateY(-1000px); /* Balloons float up and slightly to the side */
      }
    }

    /* Cake Styles */
    .birthday-cake {
      position: absolute;
      bottom: 20%;
      left: 50%;
      transform: translateX(-50%);
      width: 200px;
      height: 150px;
      background-color: #f9c1e6;
      border-radius: 20px 20px 0 0;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .cake-layer {
      background-color: #ff1493;
      height: 40px;
      margin-top: 10px;
      border-radius: 5px;
    }

    .cake-top {
      background-color: #fff;
      height: 30px;
      border-radius: 5px;
      position: relative;
    }

    .cake-candle {
      position: absolute;
      top: 5px;
      left: 50%;
      transform: translateX(-50%);
      width: 10px;
      height: 30px;
      background-color: yellow;
      border-radius: 50%;
      animation: flicker 0.5s infinite alternate;
    }

    @keyframes flicker {
      0% {
        height: 30px;
      }
      100% {
        height: 40px;
      }
    }

    /* Text "I love you sooooooo much" at the bottom */
    .love-text {
      position: fixed;
      bottom: 10px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 2em;
      color: #ffeb3b;
      font-weight: bold;
      animation: pulseText 2s ease-in-out infinite;
    }

    /* Text pulse animation */
    @keyframes pulseText {
      0% {
        transform: translateX(-50%) scale(1);
      }
      50% {
        transform: translateX(-50%) scale(1.2);
      }
      100% {
        transform: translateX(-50%) scale(1);
      }
    }

    /* Countdown Timer Styles */
    .timer {
      font-size: 2em;
      color: #fff;
      font-weight: bold;
      margin-top: 50px;
      animation: blink 1s infinite;
    }

    /* Blinking effect for timer */
    @keyframes blink {
      0% {
        opacity: 1;
      }
      50% {
        opacity: 0.6;
      }
      100% {
        opacity: 1;
      }
    }

  </style>
</head>
<body>
  <div class="container">
    <h1>Happy Birthday, Mohtarma Ji!</h1>
    <p>I love you so much!</p>
  </div>

  <!-- Balloon Elements -->
  <div class="balloon" style="left: 10%;"></div>
  <div class="balloon" style="left: 30%;"></div>
  <div class="balloon" style="left: 50%;"></div>
  <div class="balloon" style="left: 70%;"></div>
  <div class="balloon" style="left: 90%;"></div>

  <!-- Large Heart in the Background -->
  <div class="big-heart"></div>

  <!-- Small Heart Elements -->
  <div class="small-heart"></div>
  <div class="small-heart"></div>
  <div class="small-heart"></div>
  <div class="small-heart"></div>
  <div class="small-heart"></div>

  <!-- Birthday Cake -->
  <div class="birthday-cake">
    <div class="cake-top">
      <div class="cake-candle"></div>
    </div>
    <div class="cake-layer"></div>
    <div class="cake-layer"></div>
    <div class="cake-layer"></div>
  </div>

  <!-- Text "I love you sooooooo much" -->
  <div class="love-text">I love you sooooooo much!</div>

  <!-- Timer Countdown -->
  <div class="timer" id="countdown"></div>

  <!-- Sound for heart beat -->
  <audio id="heartBeatSound" src="https://www.soundjay.com/button/beep-07.wav" loop></audio>

  <!-- Background Birthday Song -->
  <audio id="birthdaySong" src="https://www.soundjay.com/button/beep-09.wav" loop autoplay></audio>

  <script>
    // Timer Countdown for February 1, 2025
    const countdownElement = document.getElementById('countdown');
    const targetDate = new Date("February 1, 2025 00:00:00").getTime();

    function updateTimer() {
      const now = new Date().getTime();
      const distance = targetDate - now;

      // Calculate time components
      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);

      // Display the timer
      countdownElement.innerHTML = `${days}d ${hours}h ${minutes}m ${seconds}s`;

      // If the countdown is over, display a message
      if (distance < 0) {
        countdownElement.innerHTML = "Happy Birthday!";
      }
    }

    // Update the timer every second
    setInterval(updateTimer, 1000);

  </script>
</body>
</html>
